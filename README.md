# ServeConnect-home

This repository contains the full-stack codebase for ServeConnect.

## Structure
- **frontend**: React + Vite + Tailwind CSS (configured for deployment on Vercel)
- **backend**: Node.js + Express + Socket.io + MongoDB Atlas (designed to run on a persistent environment like Render, Railway, or Fly.io)
